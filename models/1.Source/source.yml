version: 2
 
sources:
  - name: demo_sources
    description: Demo source tables from data lake
    database:  |
      {%- if  target.name == "dev" -%} SQL_PRACTICE
      {%- elif target.name == "qa"  -%} SQL_PRACTICE
      {%- elif target.name == "uat"  -%} SQL_PRACTICE
      {%- elif target.name == "prod"  -%}  SQL_PRACTICE
      {%- else -%} invalid_database
      {%- endif -%}
    schema: SPORTS
    tables:
      - name: teams
        description: Customized purchase order data with line-level details